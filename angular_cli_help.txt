Crear componente:
	ng generate component rutarelativa/<nombre> --skip-tests -is
			--skip-tests suprime archivos de tests
			-is suprime csss
	
Crear módulo:
	ng generate module rutarelativa/<nombre>
	
Crear servicio:
	ng generate service rutarelativa/<nombre> --skip-tests

